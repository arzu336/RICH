{
  "framework": "vite",
  "buildCommand": "npm run --prefix web-frontend build",
  "installCommand": "npm install --prefix web-frontend",
  "outputDirectory": "web-frontend/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}